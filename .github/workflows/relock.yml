name: relock

on:
  workflow_dispatch:
  issue_comment:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: checkout
        uses: actions/checkout@1af3b93b6815bc44a9784bd300feb67ff0d1eeb3

      - name: run
        uses: conda-incubator/relock-conda@pull/445/head
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          git-user-name: '5ad58126-1298-4353-1fce-431c5af27be8'

