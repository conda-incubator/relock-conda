name: relock

on:
  workflow_dispatch:
  issue_comment:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: run
        uses: conda-incubator/relock-conda@pull/307/head
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          git-user-name: '7a42aee5-8911-d5f1-28bf-40df3519eab1'

