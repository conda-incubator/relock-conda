name: relock

on:
  workflow_dispatch:
  issue_comment:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: run
        uses: conda-incubator/relock-conda@pull/341/head
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          git-user-name: '17513892-7430-0e0e-0ed8-0fc5f76ed0ab'

