name: relock

on:
  workflow_dispatch:
  issue_comment:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: checkout
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: run
        uses: conda-incubator/relock-conda@pull/355/head
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          git-user-name: '2e0cd7e4-61ec-c449-dad5-4023cc19ea9d'

