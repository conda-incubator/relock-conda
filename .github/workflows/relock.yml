name: relock

on:
  workflow_dispatch:
  issue_comment:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8

      - name: run
        uses: conda-incubator/relock-conda@pull/423/head
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          git-user-name: '11064520-1a67-b0c6-d80e-6953306df38a'

