name: relock

on:
  workflow_dispatch:
  issue_comment:

jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
      - name: checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: run
        uses: conda-incubator/relock-conda@pull/456/head
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          git-user-name: 'fc367ab8-59ab-4600-8850-622f73d1b296'

